# streaming

Kafka care-events consumer, covering the two event-driven metrics
from the gold layer design (care line allocation gap, out-of-range
lab trend), with retry, dead-letter queue and idempotent processing.
Added in a later step.
