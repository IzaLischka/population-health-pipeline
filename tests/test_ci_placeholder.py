def test_ci_pipeline_runs():
    """
    Placeholder so pytest has something to collect before the first real
    domain test exists. Remove this once a real test suite (starting with
    the Base class tests) lands in the repository.
    """
    assert True